page {
  background-color: #f2f2f7;
  font-family: -apple-system, 'SF Pro', 'Helvetica Neue', sans-serif;
  color: #000000;
  font-size: 14px;
  line-height: 1.4;
  padding-bottom: 20px;
  padding-top: 44px; /* 为导航栏预留空间 */
}

/* iOS风格的弹出对话框 */
.weui-half-screen-dialog_large {
  top: auto;
  border-top-left-radius: 12px;
  border-top-right-radius: 12px;
  overflow: hidden;
  background-color: #f2f2f7;
}

.weui-half-screen-dialog__hd {
  display: flex;
  align-items: center;
  padding: 8px 24px;
  position: relative;
}

.wrap {
  margin-top: 0;
}

/* iOS风格的操作栏 */
.ios-action-bar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  background-color: #f2f2f7;
  z-index: 1000;
}

.ios-status-bar {
  height: 20px; /* 状态栏高度 */
}

.ios-action-content {
  display: flex;
  justify-content: flex-end;
  align-items: center;
  height: 44px;
  padding: 0 16px;
}

.spacer {
  flex: 1;
}

.ios-add-button {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #36b4ff, #1e90ff);
  border-radius: 18px;
  color: white;
  box-shadow: 0 2px 8px rgba(54, 180, 255, 0.3);
  transition: all 0.2s ease;
}

.ios-add-button:active {
  transform: scale(0.95);
  background: linear-gradient(135deg, #2aa0e5, #1880e0);
}

.ios-icon {
  font-size: 24px;
  font-weight: 300;
  line-height: 24px;
}

/* 纪念日列表样式 */
.anniversary-list {
  width: calc(100% - 32px);
  margin: 16px auto 0;
  padding-top: 50px; /* 为操作栏预留空间 */
}

.anniversary-card {
  position: relative;
  width: 100%;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
  margin-bottom: 16px;
  padding: 16px;
  overflow: hidden;
}

.anniversary-content {
  display: flex;
  flex-direction: column;
}

.anniversary-title {
  font-size: 16px;
  font-weight: normal;
  color: #333333;
  margin-bottom: 4px;
}

.anniversary-date {
  font-size: 13px;
  font-weight: normal;
  color: #333333;
  margin-bottom: 6px;
}

.anniversary-days {
  font-size: 16px;
  color: #333333;
}

.anniversary-days text {
  font-size: 20px;
  font-weight: 500;
  color: #ff6b8b;
  margin-right: 4px;
}

/* 删除按钮样式 */
.ios-delete-button {
  position: absolute;
  right: 8px;
  top: 8px;
  width: 24px;
  height: 24px;
  border-radius: 12px;
  background-color: rgba(0, 0, 0, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;
}

/* 关闭按钮样式 */
.ios-close-btn {
  width: 30px;
  height: 30px;
  border-radius: 15px;
  background-color: #e5e5ea;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 8px;
}

.ios-close-icon {
  font-size: 24px;
  color: #8e8e93;
  line-height: 24px;
}

.ios-delete-icon {
  font-size: 18px;
  color: #ffffff;
  line-height: 18px;
}

/* 对话框标题样式 */
.weui-half-screen-dialog__hd__side {
  position: absolute;
  left: 8px;
  top: 50%;
  transform: translateY(-50%);
}

.weui-half-screen-dialog__hd__main {
  display: flex;
  justify-content: center;
  width: 100%;
}

.weui-half-screen-dialog__title {
  font-size: 17px;
  font-weight: 600;
  color: #000;
  text-align: center;
}

/* 表单样式 */
.weui-half-screen-dialog__bd {
  padding: 16px;
}

.ios-form-group {
  background-color: #ffffff;
  border-radius: 10px;
  overflow: hidden;
  margin-bottom: 16px;
}

.ios-form-item {
  padding: 12px 16px;
  border-bottom: 0.5px solid rgba(0, 0, 0, 0.1);
}

.ios-form-item:last-child {
  border-bottom: none;
}

.ios-input {
  height: 24px;
  line-height: 24px;
  font-size: 15px;
  width: 100%;
}

.ios-input-placeholder {
  color: #8e8e93;
}

/* 按钮样式 */
.ios-button {
  border-radius: 10px;
  font-size: 16px;
  font-weight: 500;
  height: 44px;
  line-height: 44px;
  text-align: center;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 16px;
}

.ios-button-primary {
  background: linear-gradient(135deg, #36b4ff, #1e90ff);
  color: #ffffff;
}

.ios-button-primary:active {
  background: linear-gradient(135deg, #2aa0e5, #1880e0);
  opacity: 0.9;
}
