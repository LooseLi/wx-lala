@import "weui.wxss";

page,
view,
text,
image,
navigator {
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}

/* 主题颜色 */
page {
  --backgroundColorButton: #e95f6f;
  --fontColor: #e95f6f;
  font-size: 28rpx;
}

/* 字体大小 */